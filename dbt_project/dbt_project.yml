name: 'realtime_analytics'
version: '1.0.0'
config-version: 2

profile: 'realtime_profile'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  realtime_analytics:
    gold:
      +materialized: table
